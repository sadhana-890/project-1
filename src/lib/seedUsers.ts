import { mockUsers, MockUser } from "./mockUsers";

/* export function seedUsers() {
  if (mockUsers.length > 0) {
    console.log(" Users already seeded");
    return;
  }

  const defaultUsers: MockUser[] = [
    { id: 1, name: "Alice", password: "1234", role: "user" },
    { id: 2, name: "Bob", password: "admin123", role: "admin" },
    { id: 3, name: "Charlie", password: "super123", role: "superadmin" },
  ];

  mockUsers.push(...defaultUsers);

  console.log("Users seeded:", mockUsers);
} */
